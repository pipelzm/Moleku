"""
Moleku internal modules (package name kept as ``mcrg`` for backwards compatibility).

Keep `mcrg_desktop.py` as the GUI entrypoint for now, while moving
stable logic into importable modules under `mcrg/`.
"""

